import React, { Component } from 'react'

class Login extends Component {
  handleSubmit = (val) => {
    console.log(val)
  }
  render() {
    return (
      <div>
        names
      </div>
    )
  }
}

export default Form.create()(Login)
